The project now uses MySQL instead of SQLite.

For XAMPP:
1. Start Apache and MySQL.
2. Import database/elite_event_mysql.sql in phpMyAdmin, OR simply open the website once; the PHP installer creates the database/tables automatically.
3. MySQL defaults are in config/database.php (root with blank password).
